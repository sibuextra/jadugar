document.addEventListener("DOMContentLoaded", (function() {
    try {
        document.getElementById("top").addEventListener("click", (function() {
            window.scrollTo({
                top: 0,
                behavior: "smooth"
            })
        }))
    } catch (e) {
        console.error("Error setting up the back button, ignore if this is not the Transcript Page.")
    }
    try {
        var e = "Unknown"; - 1 != window.navigator.userAgent.indexOf("Windows NT 10.0") && (e = "Windows"), -1 != window.navigator.userAgent.indexOf("Mac") && (e = "MacOS"), -1 != window.navigator.userAgent.indexOf("Linux") && (e = "Linux");
        var o = document.querySelectorAll(".macos"),
            t = document.querySelectorAll(".window"),
            n = document.querySelectorAll(".default");
        "MacOS" === e ? o.forEach((function(e) {
            e.classList.remove("hidden"), e.classList.contains("inline-block") || e.classList.add("inline")
        })) : "Windows" === e ? t.forEach((function(e) {
            e.classList.remove("hidden"), e.classList.contains("inline-block") || e.classList.add("inline")
        })) : n.forEach((function(e) {
            e.classList.remove("hidden"), e.classList.contains("inline-block") || e.classList.add("inline")
        }))
    } catch (e) {
        console.error("An error occurred:", e);
        try {
            (n = document.querySelectorAll(".default")) && n.forEach((function(e) {
                e.classList.remove("hidden"), e.classList.add("inline")
            }))
        } catch (e) {
            console.error("An error occurred:", e)
        }
    }
    try {
        document.querySelectorAll("input[name=youtube_url]").forEach((function(e) {
            e.addEventListener("input", (function(o) {
                e.validity.patternMismatch ? e.setCustomValidity("Please enter a valid YouTube URL.") : e.setCustomValidity("")
            }))
        }))
    } catch (e) {
        console.error("An error occurred while setting Custom Validity Error for Form:", e)
    }
    try {
        document.querySelectorAll("form").forEach((function(e) {
            e.addEventListener("submit", (function() {
                var o = e.querySelector('button[type="submit"]');
                o.querySelector(".loading").classList.remove("hidden"), o.disabled = !0
            }))
        }))
    } catch (e) {
        console.error("An error occurred:", e)
    }
    try {
        document.addEventListener("keydown", (function(e) {
            (e.metaKey || e.ctrlKey) && "d" === e.key && (console.log("Bookmarked!"), window.setCookie("exit", "1", 180))
        }))
    } catch (e) {
        console.error("Error setting up the bookmark analytics.")
    }
}));