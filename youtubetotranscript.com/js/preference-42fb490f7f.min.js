function setCookie(e, t, n) {
    var o = "";
    if (n) {
        var i = new Date;
        i.setTime(i.getTime() + 24 * n * 60 * 60 * 1e3), o = "; expires=" + i.toUTCString()
    }
    return document.cookie = e + "=" + (t || "") + o + "; path=/", !0
}

function getCookie(e) {
    for (var t = e + "=", n = document.cookie.split(";"), o = 0; o < n.length; o++) {
        for (var i = n[o];
            " " == i.charAt(0);) i = i.substring(1, i.length);
        if (0 == i.indexOf(t)) return i.substring(t.length, i.length)
    }
    return null
}
document.addEventListener("DOMContentLoaded", (function() {
    try {
        var e = document.getElementById("theme-switcher"),
            t = document.documentElement.getAttribute("data-theme");
        e.checked = "black" === t, e.addEventListener("change", (function() {
            var e = this.checked ? "black" : "bumblebee";
            setCookie("website-theme", e, 90), document.documentElement.setAttribute("data-theme", e)
        }))
    } catch (e) {
        console.error("Error in theme switcher: ", e)
    }
})), window.setCookie = setCookie, window.getCookie = getCookie;